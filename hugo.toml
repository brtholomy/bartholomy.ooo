baseURL = "https://www.bartholomy.ooo/"
defaultContentLanguage = "en"
title = "bartholomy"
theme = "bth-hermit"

# use &#169; {{ now.Format "2006" }} preceeding:
copyright = "hautogdoad"

# disabled : stop tracking my readers for fucks sake.
# click tracking was added to content like this:
# https://github.com/brtholomy/bartholomy.ooo/commit/f9209dc
# removed:
# https://github.com/brtholomy/bth-hermit/commit/5de238b
# https://github.com/brtholomy/bth-hermit/commit/58d5ef9
# googleAnalytics = "G-CXJ7N79PMM"

# no effect in my custom rss template, currently.
# rssLimit = 10  # Maximum number of items in the RSS feed.

# Keep images in assets/ now, so that I can load them with resources.Get, and
# then access .MediaType etc. By extending the static dir like this, I can
# access images in any doc with plain markdown, as though it were loaded into
# the static dir.
staticDir = ['static', 'assets']

[params.author]
  name = "Bartholomy"
  url = "/about"
  email = "yo@bartholomy.ooo"

[params.theme]
  default = "redeye"
  home = "redeye"
  # WARN: theme may be set in content/posts/_index.md and content/books/_index.md etc.
  # this is intended as a fallback. it currently looks good in about/ and /study etc.
  section = "yellowjacket"
  metaTagThemeColor = "#d43"

[params]

  # NOTE: .Site.GetPage needs filename, not slug:
  # if the post is draft: true, this will currently not break the build, but it won't show up in posts/
  featuredPost = 'posts/339.ktb.md'

  # meta stuff
  homeSubtitle = "Unlimited Human Renaissance"

  # Page subtitle
  # .Page.Params.Summary := description as subtitle
  # .Page.Params.Description := description without subtitle
  #
  # NOTE: <meta> tags set in baseof.html: .Description before .Summary before .Site.Description
  # NOTE: <meta itemprop=""> values and such are filled by structured-data.html, which uses internal templates.
  #
  # NOTE: .Page.Summary will be autogenerated by hugo if missing. Relevant to the RSS generation logic.
  # But I've since changed to check .Page.Params.Summary, which is different.
  # NOTE: so without either Summary or Description set in params, RSS <item> will lack <description> tag,
  # but the <meta> tags in the header will still be present.
  description = "Moral critique, neuroscience, psychoanalysis, anthropology, and philosophy."
  favicon = "headshot-zoom.ico"
  images = ["headshot.png", "covers/0152.witch_hunt.jpg"]

  # footer
  logo = "headshot_150px.webp"
  logolink = "/about"
  copyrightlink = "/about/privacy/"
  defaultPlace = "Fool's Bluff, Nevada"

  # NOTE: see ./themes/bth-hermit/layouts/posts/single.html for the action-button inclusion.
  # NOTE: changed to /subscribe:
  # https://github.com/brtholomy/bth-hermit/commit/29948aa
  # override this in frontmatter with studyButton: "foo"
  defaultStudyButton = "study with bartholomy"
  # override this in frontmatter with subscribeButton: "foo"
  defaultSubscribeButton = "subscribe"

  # flags
  code_copy_button = false

  # dates
  dateformNum     = "2006.01.02"
  dateformRFC822 = "Mon, 02 Jan 2006 15:04:05 -0700"

[taxonomies]
  tag = "tags"

[related]
  includeNewer = true
  threshold = 80
  toLower = false
[[related.indices]]
  name = 'tags'
  weight = 80
[[related.indices]]
  name = 'date'
  weight = 10

[menu]
  [[menu.main]]
    name = "posts"
    url = "posts/"
    weight = 10

  [[menu.main]]
    name = "books"
    url = "books/"
    weight = 20

# NOTE: disable for now
  # [[menu.main]]
  #   name = "music"
  #   url = "music/"
  #   weight = 30

# NOTE: disable for now
  # [[menu.main]]
  #   name = "study"
  #   url = "study/"
  #   weight = 30

  [[menu.main]]
    name = "about"
    url = "about/"
    weight = 40

  # nested list for reference should I need it:
  # parent = ""
  # https://gohugo.io/content-management/menus/#example-site-configuration
  # [[menu.main]]
  #   name = "support"
  #   url = "support/"
  #   parent = "about"

[markup]
defaultMarkdownHandler = 'goldmark'

[markup.goldmark.extensions]
definitionList = true
footnote = true
linkify = true
linkifyProtocol = 'https'
strikethrough = true
table = true
taskList = true

# failing, but this is kinda bleeding edge, so that may be it
[markup.goldmark.extensions.typographer]
# double dash -- makes mdash:
enDash = '&mdash;'

# no collapsed &hellip;
ellipsis = '...'

# I don't like smart quotes:
rightDoubleQuote = '"'
rightSingleQuote = "'"
rightAngleQuote = "'"
leftDoubleQuote = '"'
leftSingleQuote = "'"
leftAngleQuote = "'"
apostrophe = "'"

# german style:
# leftDoubleQuote = '&bdquo;'
# rightDoubleQuote = '&rdquo;'

##########
# defaults:
# apostrophe = '&rsquo;'
# ellipsis = '&hellip;'
# emDash = '&mdash;'
# enDash = '&ndash;'

# leftAngleQuote = '&laquo;'
# leftDoubleQuote = '&ldquo;'
# leftSingleQuote = '&lsquo;'

# rightAngleQuote = '&raquo;'
# rightDoubleQuote = '&rdquo;'
# rightSingleQuote = '&rsquo;'

[markup.goldmark.parser]
autoHeadingID = true
autoHeadingIDType = 'github'
wrapStandAloneImageWithinParagraph = true

[markup.goldmark.parser.attribute]
block = false
title = true

[markup.goldmark.renderer]
hardWraps = false
unsafe = false
xhtml = false

# https://gohugo.io/getting-started/configuration-markup/#highlight
[markup.highlight]
style = 'gruvbox'

# HTTP headers
# themes/bth-hermit/layouts/index.headers
# we do this rather than a [[headers]] entry in netlify.toml because we need access to the inline JS for the CSP hash.
# https://gohugo.io/configuration/media-types/
# https://docs.netlify.com/build/configure-builds/file-based-configuration/#headers
# TODO: i don't really understand why this works without defining a "headers" suffix.
[mediaTypes]
  [mediaTypes."text/netlify"]
    suffixes = [""]
    delimiter = ""

# https://gohugo.io/configuration/output-formats/
# outputs public/_headers for Netlify to find: https://docs.netlify.com/manage/routing/headers/
[outputFormats]
  [outputFormats.Headers]
    mediaType = "text/netlify"
    baseName = "_headers"
    isPlainText = true
    notAlternative = true

  [outputFormats.PlainText]
    mediaType = "text/plain"
    baseName = "index"
    isPlainText = true

[outputs]
  home = ["HTML", "Headers"]
  page = ["HTML", "PlainText"]
  # default, but to be explicit about why the template works: layouts/posts/rss.xml
  section = ['HTML', 'RSS']
